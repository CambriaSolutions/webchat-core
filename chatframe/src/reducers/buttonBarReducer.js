import * as t from '../actions/actionTypes'

const initialState = {
  visible: false,
  buttons: []
}
function config(state = initialState, action) {
  switch (action.type) {
    default:
      return state
  }
}

export default config
